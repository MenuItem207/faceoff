name: client
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  google_fonts: ^3.0.1
  dargon2_flutter: ^3.1.0 # hash passwords
  socket_io_client: ^2.0.0 
  image_picker_web: ^2.1.1
  mime_type: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/unflexed.png
    - assets/flexed.png
